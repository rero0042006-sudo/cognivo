# Local server (optional)

AI distractor images are generated by the Cloudflare Worker, not this folder:

https://memory-moments-ai.rero0042006.workers.dev/

Android talks to that Worker over HTTPS. Family photos never leave the device.

`npm start` here only runs a health endpoint for local checks.
